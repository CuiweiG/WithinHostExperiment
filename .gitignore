.Rproj.user
.Rhistory
.RData
.Ruserdata
*.o
*.so
*.dll
*.Rproj
.DS_Store
doc/
Meta/
inst/doc/
# The figure run's provenance files are tracked so that the README can be
# checked against the figures without re-running it; the vector copies are not.
readme_figure_output/*
!readme_figure_output/*.csv
!readme_figure_output/sessionInfo.txt
