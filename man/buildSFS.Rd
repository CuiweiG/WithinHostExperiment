% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sfs.R
\name{buildSFS}
\alias{buildSFS}
\title{Build a site frequency spectrum from a WithinHostExperiment}
\usage{
buildSFS(
  whe,
  sampleIdx = 1L,
  nBins = 20L,
  fold = TRUE,
  usePassedOnly = TRUE,
  threshold = 0.03,
  genomeLength = NULL
)
}
\arguments{
\item{whe}{A \code{\link{WithinHostExperiment}}.}

\item{sampleIdx}{Integer or character. Which sample (column) to
use. Default: 1.}

\item{nBins}{Integer. Number of frequency bins (default: 20).}

\item{fold}{Logical. If \code{TRUE}, fold the SFS to minor
allele frequency (0 to 0.5). Default: \code{TRUE}.}

\item{usePassedOnly}{Logical. If \code{TRUE}, only use QC-passed
variants. Default: \code{TRUE}.}

\item{threshold}{Numeric. Detection threshold (default: 0.03).
The first bin starts at this value, not 0.}

\item{genomeLength}{Integer (optional). Reference genome length.}
}
\value{
A \code{\link{WithinHostSFS}} object.
}
\description{
Constructs a \code{\link{WithinHostSFS}} object from the allele
frequencies in a \code{\link{WithinHostExperiment}}. One SFS is
built per sample.
}
\examples{
vcf <- system.file("extdata", "test_donor.vcf",
    package = "WithinHostExperiment")
whe <- readWithinHost(vcf,
    colData = S4Vectors::DataFrame(sample_id = "donor"),
    caller = "ivar")
sfs <- buildSFS(whe, genomeLength = 1000L)
sfs
}
