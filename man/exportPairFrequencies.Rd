% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridge.R
\name{exportPairFrequencies}
\alias{exportPairFrequencies}
\title{Export donor-recipient frequency table}
\usage{
exportPairFrequencies(whe, pairId = NULL, usePassedOnly = TRUE)
}
\arguments{
\item{whe}{A \code{\link{WithinHostExperiment}} with \code{role}
and \code{pair_id} in \code{colData}.}

\item{pairId}{Character (optional). Specific pair to export.
If NULL, exports all pairs.}

\item{usePassedOnly}{Logical. Use only QC-passed variants
(default: TRUE).}
}
\value{
A \code{DataFrame} with columns: chrom, position, ref, alt,
  donor_freq, recipient_freq, shared (logical).
}
\description{
For each transmission pair, creates a table of variant frequencies
in the donor and recipient.
}
\examples{
vcf1 <- system.file("extdata", "test_donor.vcf",
    package = "WithinHostExperiment")
vcf2 <- system.file("extdata", "test_recipient.vcf",
    package = "WithinHostExperiment")
whe <- readWithinHost(c(vcf1, vcf2),
    colData = S4Vectors::DataFrame(
        sample_id = c("donor", "recipient"),
        role = c("donor", "recipient"),
        pair_id = c("p1", "p1")),
    caller = "ivar")
exportPairFrequencies(whe)
}
