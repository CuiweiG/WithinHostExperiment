% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{WithinHostExperiment}
\alias{WithinHostExperiment}
\title{Create a WithinHostExperiment from components}
\usage{
WithinHostExperiment(assays, rowRanges, colData, metadata = list())
}
\arguments{
\item{assays}{Named list of matrices. Must include at least
\code{altFreq}. If \code{qcPass} is not provided, it is
automatically created as an all-TRUE matrix.}

\item{rowRanges}{\code{GRanges}. Genomic coordinates for each
variant site.}

\item{colData}{\code{DataFrame} or \code{data.frame}. Sample
metadata. Must contain a \code{sample_id} column.}

\item{metadata}{Named list (optional). Additional metadata such as
\code{reference_genome}, \code{genome_length}, \code{calling_info}.}
}
\value{
A \code{\link{WithinHostExperiment}} object.
}
\description{
Constructs a \code{\link{WithinHostExperiment}} from assay matrices,
genomic ranges, and sample metadata.
}
\examples{
library(GenomicRanges)
gr <- GRanges("seg1", IRanges::IRanges(c(100, 200, 300), width = 1))
whe <- WithinHostExperiment(
    assays = list(altFreq = matrix(c(0.1, 0.2, 0.3), ncol = 1)),
    rowRanges = gr,
    colData = S4Vectors::DataFrame(sample_id = "S1"))
whe
}
