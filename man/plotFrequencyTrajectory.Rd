% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/longitudinal.R
\name{plotFrequencyTrajectory}
\alias{plotFrequencyTrajectory}
\title{Plot allele frequency trajectories over time}
\usage{
plotFrequencyTrajectory(
  whe,
  hostId,
  hostCol = "host_id",
  timeCol = "timepoint",
  usePassedOnly = TRUE
)
}
\arguments{
\item{whe}{A \code{\link{WithinHostExperiment}} with longitudinal
samples.}

\item{hostId}{Character scalar. Host identifier to plot.}

\item{hostCol}{Character scalar. Column name in \code{colData}
for host identifier (default: \code{"host_id"}).}

\item{timeCol}{Character scalar. Column name in \code{colData}
for time point (default: \code{"timepoint"}).}

\item{usePassedOnly}{Logical. If TRUE (default), only use
QC-passed variants.}
}
\value{
A ggplot object.
}
\description{
Creates a ggplot2 spaghetti plot of allele frequency over time
for each variant within a specified host. Each line represents
one variant.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
    gr <- GenomicRanges::GRanges("seg1",
        IRanges::IRanges(c(100, 200), width = 1))
    S4Vectors::mcols(gr)$ref <- c("A", "C")
    S4Vectors::mcols(gr)$alt <- c("T", "G")
    freq <- matrix(c(0.05, 0.10, 0.15, 0.20), nrow = 2, ncol = 2)
    whe <- WithinHostExperiment(
        assays = list(altFreq = freq),
        rowRanges = gr,
        colData = S4Vectors::DataFrame(
            sample_id = c("S1_t1", "S1_t2"),
            host_id = c("H1", "H1"),
            timepoint = c(1, 2)))
    plotFrequencyTrajectory(whe, hostId = "H1")
}
}
